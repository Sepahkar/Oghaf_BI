# 🏛️ داشبورد مدیریتی سازمان اوقاف

یک داشبورد جامع برای مدیریت موقوفات با قابلیت نمایش سلسله مراتبی از استان تا رقبات

## 🚀 راهنمای اجرا

### مرحله ۱: نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### مرحله ۲: اجرای سرور
```bash
python app.py
```

### مرحله ۳: باز کردن مرورگر
```
http://localhost:5000
```

## 🔧 راهنمای دیباگ

### اگر صفحه در حالت بارگذاری می‌ماند:

**۱. بررسی کنسول مرورگر:**
- `F12` را فشار دهید
- به تب `Console` بروید
- خطاهای JavaScript را بررسی کنید

**۲. بررسی Network:**
- در Developer Tools به تب `Network` بروید
- صفحه را Refresh کنید
- بررسی کنید که آیا `/api/provinces` با کد ۲۰۰ پاسخ می‌دهد

**۳. بررسی لاگ سرور:**
در ترمینال که سرور اجرا می‌شود، باید این خطوط را ببینید:
```
127.0.0.1 - - [تاریخ] "GET / HTTP/1.1" 200 -
127.0.0.1 - - [تاریخ] "GET /api/provinces HTTP/1.1" 200 -
```

### مشکلات رایج و راه حل:

**مشکل ۱: صفحه سفید نمایش داده می‌شود**
```bash
# بررسی کنید که سرور روشن است
python app.py
```

**مشکل ۲: خطای ۴۰۴ برای فایل‌های static**
```bash
# بررسی کنید که فایل‌ها وجود دارند
ls static/css/
ls static/js/
```

**مشکل ۳: خطای JavaScript**
- مرورگر را Refresh کنید
- Cache مرورگر را پاک کنید (`Ctrl+Shift+R`)

### دستورات دیباگ مفید:

**۱. بررسی وضعیت سرور:**
```bash
curl http://localhost:5000/api/provinces
```

**۲. بررسی فایل‌های static:**
```bash
curl http://localhost:5000/static/js/dashboard.js
```

**۳. مشاهده لاگ‌های تفصیلی:**
در `app.py` خط زیر را اضافه کنید:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```

## 📱 نحوه استفاده

### سطح کشور:
- نمایش نقشه ایران
- آمار کلی موقوفات
- جدول استان‌ها

### سطح استان:
- کلیک روی "مشاهده شهرستان‌ها"
- یا کلیک روی نام استان در جدول

### سطح شهرستان:
- کلیک روی "مشاهده موقوفات"

### سطح موقوفات:
- کلیک روی "مشاهده رقبات"

### ناوبری:
- **دکمه بازگشت**: برگشت به سطح قبلی
- **مسیر ناوبری**: کلیک روی هر سطح برای رفتن مستقیم

## 🎯 ویژگی‌ها

- ✅ نمایش سلسله مراتبی داده‌ها
- ✅ نقشه تعاملی با Leaflet
- ✅ طراحی ریسپانسیو با Bootstrap 5
- ✅ پشتیبانی کامل از RTL و فارسی
- ✅ آمار لحظه‌ای در هر سطح
- ✅ فایل‌های محلی (بدون وابستگی به اینترنت)

## 🛠️ ساختار پروژه

```
Oghaf_BI/
├── app.py                     # سرور Flask
├── requirements.txt           # وابستگی‌های Python
├── templates/
│   └── dashboard.html         # قالب اصلی
├── static/
│   ├── css/                   # فایل‌های CSS
│   └── js/                    # فایل‌های JavaScript
└── README_FA.md              # این فایل
```

## 📊 داده‌های نمونه

پروژه شامل داده‌های نمونه برای:
- **۳ استان**: تهران، اصفهان، فارس
- **شهرستان‌های مختلف** در هر استان
- **موقوفات متنوع** با اطلاعات کامل
- **رقبات تفصیلی** با وضعیت اجاره

## 🔗 API Endpoints

- `GET /` - صفحه اصلی
- `GET /api/provinces` - لیست استان‌ها
- `GET /api/province/{id}/cities` - شهرستان‌های یک استان
- `GET /api/province/{id}/city/{id}/endowments` - موقوفات یک شهر
- `GET /api/province/{id}/city/{id}/endowment/{id}/properties` - رقبات یک موقوفه

## 💡 نکات مهم

- سرور باید همیشه روشن باشد
- برای تغییرات در کد، سرور را restart کنید
- برای مشکلات نمایش، cache مرورگر را پاک کنید
- تمام فایل‌های مورد نیاز به صورت محلی موجود است

---
**ساخته شده برای سازمان اوقاف و امور خیریه** 🕌